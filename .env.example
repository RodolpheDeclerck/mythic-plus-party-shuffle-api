# Application and integration tests (npm run test:integration).
# In CI, these are set by .github/workflows/prd-to-pr.yml (Postgres + Redis services).
REDIS_URL=

NODE_ENV=
DOMAIN=
CORS_ORIGIN=
JWT_SECRET=

POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE